# Electron's postinstall downloads a platform binary; keep it out of the store cache noise.
node-linker=hoisted
# Electron + esbuild need their postinstall to fetch platform binaries.
enable-pre-post-scripts=true
